# .readthedocs.yaml

# Required
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
    nodejs: "20"
  commands:
    - npm -g i @mermaid-js/mermaid-cli

sphinx:
  configuration: src/conf.py

python:
  install:
    - requirements: requirements.txt
